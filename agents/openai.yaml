interface:
  display_name: "Before Work"
  short_description: "Ask only about consequential ambiguity before acting"
  default_prompt: "Use $before-work to ask one Socratic question at a time only when the answer could materially change the result; otherwise follow conventions and proceed."
